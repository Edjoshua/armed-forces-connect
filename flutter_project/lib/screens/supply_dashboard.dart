import 'package:flutter/material.dart';

class SupplyDashboard extends StatelessWidget {
  const SupplyDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Supply Dashboard Content'));
  }
}